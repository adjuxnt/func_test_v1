// definering av funk
function skrivUtMin(setning) {
	console.log(setning);
};

// oppkalling av funk
skrivUtMin("Hallå från funksjone hehe");

const finSetning = "Dette er en vakker setning";

skrivUtMin(finSetning);
